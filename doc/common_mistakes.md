# Common Mistakes

- Do not edit `doc/vibetask.md` unless the user explicitly permits a specific section.
- Keep modules small and focused.
- Prefer tests and type checks before claiming a task is complete.
- Avoid browser `alert()` or `prompt()` calls; use in-app UI feedback instead.
- Keep docs in `doc/`.
